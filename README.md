# tuteTask15
### JavaScript Array functions retated Task
   * Task1(find Max number in array)
       + using comarision between elements
         - Max variable hold a arr[0] element and compare with other
 * Task2(find sum of all elements in array)
       + using add  elements one to another by for loop
         - sum += arr[i];

   * Task3(count the odd numbers in array)
       + using add  elements one to another by for loop
         - give if condition for odd one by one and count increase;
